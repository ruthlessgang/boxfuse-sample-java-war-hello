pipeline {
    agent any
    tools {
    
  }

    stages {
        stage('Build') {
            steps {
                echo 'mvn done'
                sh 'mvn package'
            }
        }
    }
}
